### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame

  ## No initialize method




  def checkforAce(card)
    if card.value = 1 #==
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2) #should read: def highest_card(card1, card2);
  if card1.value > card2.value
    return card.name # card class has no name attribute or method, should also read card1
  else
    card2 #should be card2.name or card2.name()
  end
end
end #this end should be on line 40

def self.cards_total(cards) #not a class method
  total  # total = 0
  for card in cards
    total += card.value
    return "You have a total of" + total #needs to come out of loop
  end
end


```
